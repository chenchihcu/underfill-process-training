# Underfill training twin risk ledger

| Scope | Risk | Guardrail | Next action | Revalidation | Rollback | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| Model authority | Procedural 3D scenes could be mistaken for calibrated production prediction. | UI and manifests identify controlled, training-only, experimental, and disputed authority; non-controlled modules expose no production result control. | Replace procedural geometry only after approved CAD, fixture dimensions, material data, and calibration evidence exist. | Schema/unit/E2E authority gates plus engineering review. | Restore `e0b42cde` / `2d7beb7fb081a099`. | Mitigated; calibration remains open |
| Engineering values | Training content or simulator code could duplicate or drift from controlled limits. | `process-spec.v1.json` is the sole engineering-value source; experience validation rejects out-of-band numeric content and broken rule references. | Review each future numeric change with rule ID, source, reference, version, and authority. | `npm run validate:spec` and `npm run validate:experience`. | Revert the specific specification commit. | Controlled |
| GPU performance | Transparent PBR scenes and shadows may stall software or low-end WebGL renderers. | Route-level loading, instancing, adaptive software-render quality, bounded pixel ratio, module disposal, and interactive SVG fallback. | Run a representative production-workstation and low-end GPU benchmark before claiming hardware coverage. | Nine-module desktop/mobile E2E, object-count parity, console and frame-time inspection. | Force `?fallback=1` or restore the prior artifact. | Mitigated; real workstation test open |
| Offline behavior | A dynamic simulator chunk or local media could be missing after installation. | Post-build service worker precaches the immutable artifact; offline E2E switches through all nine modules. | Repeat offline gate whenever asset routing or service-worker logic changes. | Desktop offline nine-module E2E and `verify:dist`. | Restore prior immutable artifact. | Verified locally |
| Media rights | Vendor/NASA reference imagery may carry copyright or trademark restrictions. | Manifest records local path, source URL, rights holder, bilingual caption/alt, and usage location. No authorization claim is made. | Replace any disputed asset with owned or explicitly licensed media when requested. | Manifest audit before each public release. | Remove the affected manifest entry and local asset, then rebuild both hosts. | Accepted residual risk |
| Record integrity | Preview, language, navigation, slider, quiz, or simulation interaction could create quality records. | Only explicit `記錄檢驗 / Record inspection` submits; all other handlers remain side-effect free; IndexedDB schema stays unchanged. | Preserve the explicit-action contract in future UX work. | Unit/E2E record-count and safe-rendering tests. | Revert the affected UI/controller commit; exported records remain intact. | Verified locally |
| Release parity | Netlify and GitHub Pages could serve different artifacts. | One GitHub Actions build deploys both hosts and compares commit, app/spec version, manifest, and artifact hash. | Do not accept release until live parity passes. | `npm run verify:release -- <netlify> <pages>`. | Re-run workflow with `e0b42cde`. | Pending this release |
